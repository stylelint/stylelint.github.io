{"head":{"title":"function-calc-no-unspaced-operator","description":"function-calc-no-unspaced-operator\n\nDisallow an unspaced operator within calc functions.\n\nBefore the operator, there must be a single…"},"body":"<h1 id=\"function-calc-no-unspaced-operator\"><a href=\"#function-calc-no-unspaced-operator\" class=\"phenomic-HeadingAnchor\">#</a>function-calc-no-unspaced-operator</h1>\n<p>Disallow an unspaced operator within <code>calc</code> functions.</p>\n<pre><code class=\"hljs language-css\"><span class=\"hljs-selector-tag\">a</span> { <span class=\"hljs-attribute\">top</span>: <span class=\"hljs-built_in\">calc</span>(1px + 2px); }\n<span class=\"hljs-comment\">/**               ↑\n * The space around this operator */</span></code></pre>\n<p>Before the operator, there must be a single whitespace or a newline plus indentation. After the operator, there must be a single whitespace or a newline.</p>\n<p>The following patterns are considered warnings:</p>\n<pre><code class=\"hljs language-css\"><span class=\"hljs-selector-tag\">a</span> { <span class=\"hljs-attribute\">top</span>: <span class=\"hljs-built_in\">calc</span>(1px+2px); }</code></pre>\n<pre><code class=\"hljs language-css\"><span class=\"hljs-selector-tag\">a</span> { <span class=\"hljs-attribute\">top</span>: <span class=\"hljs-built_in\">calc</span>(1px+ 2px); }</code></pre>\n<p>The following patterns are <em>not</em> considered warnings:</p>\n<pre><code class=\"hljs language-css\"><span class=\"hljs-selector-tag\">a</span> { <span class=\"hljs-attribute\">top</span>: <span class=\"hljs-built_in\">calc</span>(1px + 2px); }</code></pre>\n<pre><code class=\"hljs language-css\"><span class=\"hljs-selector-tag\">a</span> { <span class=\"hljs-attribute\">top</span>: <span class=\"hljs-built_in\">calc</span>(calc(1em * 2) / <span class=\"hljs-number\">3</span>); }</code></pre>\n<pre><code class=\"hljs language-css\">margin-top: calc(var(--some-variable) +\n  var(--some-other-variable));</code></pre>\n<pre><code class=\"hljs language-css\">margin-top: calc(var(--some-variable)\n  + var(--some-other-variable));</code></pre>\n","rawBody":"\n# function-calc-no-unspaced-operator\n\nDisallow an unspaced operator within `calc` functions.\n\n```css\na { top: calc(1px + 2px); }\n/**               ↑\n * The space around this operator */\n```\n\nBefore the operator, there must be a single whitespace or a newline plus indentation. After the operator, there must be a single whitespace or a newline.\n\nThe following patterns are considered warnings:\n\n```css\na { top: calc(1px+2px); }\n```\n\n```css\na { top: calc(1px+ 2px); }\n```\n\nThe following patterns are *not* considered warnings:\n\n```css\na { top: calc(1px + 2px); }\n```\n\n```css\na { top: calc(calc(1em * 2) / 3); }\n```\n\n```css\nmargin-top: calc(var(--some-variable) +\n  var(--some-other-variable));\n```\n\n```css\nmargin-top: calc(var(--some-variable)\n  + var(--some-other-variable));\n```\n","raw":"---\ntitle: function-calc-no-unspaced-operator\n---\n\n# function-calc-no-unspaced-operator\n\nDisallow an unspaced operator within `calc` functions.\n\n```css\na { top: calc(1px + 2px); }\n/**               ↑\n * The space around this operator */\n```\n\nBefore the operator, there must be a single whitespace or a newline plus indentation. After the operator, there must be a single whitespace or a newline.\n\nThe following patterns are considered warnings:\n\n```css\na { top: calc(1px+2px); }\n```\n\n```css\na { top: calc(1px+ 2px); }\n```\n\nThe following patterns are *not* considered warnings:\n\n```css\na { top: calc(1px + 2px); }\n```\n\n```css\na { top: calc(calc(1em * 2) / 3); }\n```\n\n```css\nmargin-top: calc(var(--some-variable) +\n  var(--some-other-variable));\n```\n\n```css\nmargin-top: calc(var(--some-variable)\n  + var(--some-other-variable));\n```\n","__filename":"user-guide/rules/function-calc-no-unspaced-operator.md","__url":"/user-guide/rules/function-calc-no-unspaced-operator/","__resourceUrl":"/user-guide/rules/function-calc-no-unspaced-operator/index.html","__dataUrl":"/user-guide/rules/function-calc-no-unspaced-operator/index.html.6ca5aae8414c2f7c592f34628476e830.json"}